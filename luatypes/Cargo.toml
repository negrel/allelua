[package]
name = "luatypes"
version = "0.1.0"
edition = "2021"

[dependencies]
full_moon = { version = "1.2.0", default-features = false, features = [
	"lua52",
	"luajit",
] }
holylib = { path = "../holylib/" }
mlua = { version = "0.10.3", features = ["luajit52"] }
